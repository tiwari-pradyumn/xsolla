[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# Coverage is on by default so a local `pytest` gates exactly as CI does.
# Escape hatch while iterating on one test: `pytest --no-cov`.
addopts = --cov
